const ExamLevel = Object.freeze({
    EASY: 0,
    MEDIUM: 1,
    HARD: 2
});

const ExamType = Object.freeze({
    READING: 0,
    LISTENING: 1,
    WRITING: 2,
    SPEAKING: 3
});

module.exports = {
  ExamLevel,
  ExamType
};
